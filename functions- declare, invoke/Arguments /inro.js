// Arrays , Functions and Objects
// params - when declare/define
// placeholders, local vars
// arguments - when invoke/callrun
// use vars/values, multiple params, undefined
const BOB = "bob";
function hello(_name) {
  console.log("hello there " + _name);
}

// great amul
hello("amul");
hello(45);
hello(BOB);
