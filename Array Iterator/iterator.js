//powerful Array Methods
// forEack, map, fillter, find, redufce
// Iterate over array - no for loop required
// Accept CALLBACK function as an argument , calls callback against each iten in a array . Reference Item in the callback Paramater.

const numbers = [0, 1, 2, 3, 4];

// show all the numbers
for (let i = 0; i < numbers.length; i++) {
  console.log(numbers[i]);
}
